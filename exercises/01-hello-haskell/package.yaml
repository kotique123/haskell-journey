name: ex01-hello-haskell
version: 0.1.0.0
dependencies:
  - base >= 4.17 && < 5

library:
  source-dirs: src
  exposed-modules:
    - Exercise

tests:
  spec:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - ex01-hello-haskell
      - hspec >= 2.11
      - hspec-discover
      - QuickCheck
