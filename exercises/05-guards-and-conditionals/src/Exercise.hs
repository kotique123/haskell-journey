module Exercise
  ( classify
  , grade
  , fizzbuzz
  , between
  , clamp
  ) where

classify :: (Ord a, Num a) => a -> String
classify = undefined

grade :: Int -> String
grade = undefined

fizzbuzz :: Int -> String
fizzbuzz = undefined

between :: Ord a => a -> a -> a -> Bool
between = undefined

clamp :: Ord a => a -> a -> a -> a
clamp = undefined
