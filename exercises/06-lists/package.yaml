name: ex06-lists
version: 0.1.0.0
dependencies:
  - base >= 4.17 && < 5

library:
  source-dirs: src
  exposed-modules:
    - Exercise
  other-modules: []

tests:
  spec:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - ex06-lists
      - hspec >= 2.11
      - hspec-discover
      - QuickCheck
