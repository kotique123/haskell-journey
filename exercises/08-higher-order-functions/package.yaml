name: ex08-higher-order-functions
version: 0.1.0.0
dependencies:
  - base >= 4.17 && < 5

library:
  source-dirs: src
  exposed-modules:
    - Exercise

tests:
  spec:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - ex08-higher-order-functions
      - hspec >= 2.11
      - hspec-discover
      - QuickCheck
