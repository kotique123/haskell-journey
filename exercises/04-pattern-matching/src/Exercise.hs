module Exercise
  ( describeList
  , fst3
  , snd3
  , thd3
  , fibonacci
  , describeNumber
  , safeHead
  ) where

describeList :: [a] -> String
describeList = undefined

fst3 :: (a, b, c) -> a
fst3 = undefined

snd3 :: (a, b, c) -> b
snd3 = undefined

thd3 :: (a, b, c) -> c
thd3 = undefined

fibonacci :: Int -> Int
fibonacci = undefined

describeNumber :: Int -> String
describeNumber = undefined

safeHead :: [a] -> Maybe a
safeHead = undefined
